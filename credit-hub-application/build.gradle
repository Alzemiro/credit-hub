// Casos de uso e portas. Depende do domínio. Framework-free: só JDK + domínio.
// A propagação de trace-context p/ as virtual threads do scatter-gather é resolvida FORA daqui,
// via Supplier<ExecutorService> context-aware injetado pelo bootstrap (ver UseCaseConfig e DECISIONS.md).
dependencies {
    implementation project(':credit-hub-domain')
}
